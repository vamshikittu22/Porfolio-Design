---
phase: 03-chapter-reader-navigation-ux
plan: 04
type: execute
wave: 3
depends_on: ["03-02", "03-03"]
files_modified:
  - app/App.tsx
  - src/pages/chapters/index.ts
autonomous: false

must_haves:
  truths:
    - "Clicking chapter card navigates to chapter reader view"
    - "Chapter content displays with header, footer, and progress bar"
    - "Prev/Next navigation works between chapters"
    - "Browser back button returns to landing page"
    - "All 6 chapters accessible and functional"
  artifacts:
    - path: "src/pages/chapters/index.ts"
      provides: "Barrel export for all chapter components"
      exports: ["Chapter01Introduction", "Chapter02Builder", "Chapter03Journey", "Chapter04Explorer", "Chapter05Thinker", "Chapter06Connection"]
    - path: "app/App.tsx"
      provides: "Chapter routing logic"
      contains: "switch.*currentChapter"
  key_links:
    - from: "app/App.tsx"
      to: "src/pages/chapters/index.ts"
      via: "import chapter components"
      pattern: "import.*chapters"
    - from: "app/App.tsx"
      to: "contexts/NavigationContext"
      via: "useNavigation for currentChapter"
      pattern: "currentChapter"
---

<objective>
Integrate all 6 chapter components into App.tsx routing and verify complete chapter reading experience.

Purpose: Connect chapter navigation system (Phase 1) with chapter reader infrastructure (Plan 03-01) and chapter content (Plans 03-02, 03-03) to create functional end-to-end reading flow. Includes human verification checkpoint.
Output: Working chapter routing system with all 6 chapters accessible from landing page, full navigation between chapters, and verified reading experience.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chapter-reader-navigation-ux/03-RESEARCH.md

# Dependencies
@.planning/phases/03-chapter-reader-navigation-ux/03-01-SUMMARY.md
@.planning/phases/03-chapter-reader-navigation-ux/03-02-SUMMARY.md
@.planning/phases/03-chapter-reader-navigation-ux/03-03-SUMMARY.md

# Files to modify
@app/App.tsx
@contexts/NavigationContext.tsx
@data/chapters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create barrel export for chapter components and integrate into App routing</name>
  <files>
    src/pages/chapters/index.ts
    app/App.tsx
  </files>
  <action>
Create src/pages/chapters/index.ts barrel export:
- Export all 6 chapter components as named exports
- Pattern: export { Chapter01Introduction } from './Chapter01Introduction';
- Repeat for all chapters 01-06
- Enables clean import: import { Chapter01Introduction, Chapter02Builder } from '@/pages/chapters'

Modify app/App.tsx to route chapters:

1. **Add chapter imports** (after existing imports, around line 16):
   - Import all 6 chapter components from '@/pages/chapters'
   - Use destructured import: import { Chapter01Introduction, Chapter02Builder, ... } from '../src/pages/chapters'

2. **Replace placeholder chapter view** (lines 208-224):
   - Remove placeholder div with "Chapter content implementation coming in Phase 3"
   - Replace with chapter routing logic using switch/case on currentChapter
   - Structure:
     ```tsx
     {currentChapter === null ? (
       <LandingPage />
     ) : (
       <>
         {currentChapter === '01-introduction' && <Chapter01Introduction />}
         {currentChapter === '02-builder' && <Chapter02Builder />}
         {currentChapter === '03-journey' && <Chapter03Journey />}
         {currentChapter === '04-explorer' && <Chapter04Explorer />}
         {currentChapter === '05-thinker' && <Chapter05Thinker />}
         {currentChapter === '06-connection' && <Chapter06Connection />}
       </>
     )}
     ```

3. **Verify ChapterTransition wrapper** (line 206):
   - Ensure ChapterTransition still wraps the routing logic
   - Transition should apply to both landing page and chapter views
   - AnimatePresence handles enter/exit animations

4. **Remove old portfolio sections** (optional cleanup):
   - Lines 5-31 import sections for old single-page layout
   - These are now composed into chapters
   - KEEP imports - they're still needed for existing code paths
   - DO NOT remove HeroSection, AboutSection, etc. imports yet
   - Mark with TODO comment: "// TODO: Remove after verifying chapter-only navigation"

5. **Verify navigation integration:**
   - currentChapter comes from useNavigation hook (line 38)
   - NavigationProvider wraps app (lines 239-241)
   - Hash changes update currentChapter automatically (from Phase 1)

Implementation notes:
- Use conditional rendering (&&) instead of switch/case for cleaner React code
- Each chapter is already wrapped in ChapterContainer (from Plans 03-02, 03-03)
- No need to duplicate container logic here
- ChapterTransition handles fade between landing and chapters
  </action>
  <verify>
npm run dev compiles successfully
TypeScript resolves all chapter imports
No console errors on startup
Visiting http://localhost:5173/#01-introduction shows chapter content
Visiting http://localhost:5173/ shows landing page
  </verify>
  <done>
src/pages/chapters/index.ts exports all 6 chapter components
App.tsx imports chapters and routes based on currentChapter
Landing page shows when currentChapter is null
Each chapter renders when currentChapter matches its ID
ChapterTransition wrapper applies to all views
TypeScript compilation passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify chapter transitions and navigation flow</name>
  <files>
    app/App.tsx
  </files>
  <action>
Add integration verification and transition testing:

1. **Verify hash-based navigation:**
   - Open browser DevTools console
   - Navigate to each chapter via URL hash: #01-introduction, #02-builder, etc.
   - Verify currentChapter updates correctly
   - Check that URL hash matches displayed chapter

2. **Verify chapter card navigation:**
   - Start at landing page (no hash)
   - Click each chapter card
   - Verify navigateToChapter('XX-chapter', 'jump') fires
   - Confirm chapter content displays
   - Check that scroll resets to top (jump navigation behavior)

3. **Verify prev/next navigation:**
   - Navigate to Chapter 2
   - Click "Previous" button → should go to Chapter 1
   - Click "Next" button → should go to Chapter 3
   - Verify sequential navigation preserves scroll position
   - Check that Chapter 1 has no "Previous" button
   - Check that Chapter 6 has no "Next" button

4. **Verify browser back/forward:**
   - Navigate: Landing → Chapter 1 → Chapter 2
   - Click browser back button → should return to Chapter 1
   - Click back again → should return to Landing
   - Click forward → should go to Chapter 1

5. **Verify sidebar/bottom sheet navigation:**
   - Click chapter numbers in sidebar (desktop) or bottom sheet (mobile)
   - Verify jump navigation to each chapter
   - Check that current chapter is highlighted
   - Verify menu closes after selection (mobile)

Error handling verification:
- Try invalid hash: #99-invalid → should stay on landing or ignore
- Try manual hash manipulation → should validate against registry
- Check console for any React errors or warnings

Performance verification:
- Monitor React DevTools for unnecessary re-renders
- Check that ChapterTransition doesn't cause layout shift
- Verify lazy sections load only when chapter is visited
- Confirm progress bar updates smoothly during scroll

Document any issues found:
- Create TODO comments for issues that should be fixed
- Note any deviations from expected behavior
- Flag performance concerns for Phase 6
  </action>
  <verify>
All navigation paths tested manually
Browser back/forward buttons work correctly
Chapter transitions are smooth
No console errors during navigation
Lazy sections load on demand
Progress bar updates during scroll
  </verify>
  <done>
Hash-based navigation verified working
Chapter card navigation tested and functional
Prev/Next buttons navigate correctly
Browser back/forward buttons work
Sidebar and bottom sheet navigation functional
All 6 chapters accessible via all navigation methods
No blocking errors found
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete chapter reading experience</name>
  <what-built>
Complete chapter-based reading experience with 6 chapters, navigation infrastructure, and progress tracking:
- Landing page with chapter cards
- Chapter reader with header, footer, and progress bar
- All 6 chapters with existing portfolio content
- Keyboard navigation (arrow keys)
- Hash-based routing with browser back/forward support
  </what-built>
  <how-to-verify>
**1. Landing Page Experience:**
   - Visit http://localhost:5173/
   - Verify 6 chapter cards display in responsive grid
   - Check glass morphism styling on cards
   - Test hover animations (if motion enabled)
   - Verify responsive layout on mobile (resize browser)

**2. Chapter Navigation:**
   - Click "Chapter 1: The Introduction" card
   - Verify chapter loads with header showing title
   - Scroll down - verify progress bar at top fills from 0-100%
   - Check footer shows "Next" button (no "Previous" on first chapter)
   - Click "Next" → verify navigates to Chapter 2

**3. Chapter Content:**
   - Verify Chapter 1 shows Hero and About sections
   - Verify Chapter 2 shows Projects and GitHub sections
   - Verify Chapter 3 shows Career and Resume sections
   - Verify Chapter 4 shows Travel section
   - Verify Chapter 5 shows Game section
   - Verify Chapter 6 shows Contact section

**4. Keyboard Navigation:**
   - Navigate to Chapter 3
   - Press Left Arrow key → should go to Chapter 2
   - Press Right Arrow key → should go to Chapter 4
   - Verify keyboard shortcuts work across all chapters

**5. Progress Bar:**
   - Open any chapter
   - Scroll to different positions (top, middle, bottom)
   - Verify progress bar fills proportionally
   - Check bar is smooth (not janky)
   - Verify bar stays at top of viewport (fixed positioning)

**6. Browser Navigation:**
   - Navigate: Landing → Ch1 → Ch2 → Ch3
   - Click browser Back button repeatedly
   - Verify returns through: Ch2 → Ch1 → Landing
   - Click Forward button
   - Verify goes forward through chapters

**7. Mobile Experience (resize browser to ~375px):**
   - Verify chapter cards stack vertically
   - Check bottom sheet navigation is accessible
   - Test chapter card tap/click
   - Verify chapter content is readable on small screen
   - Check footer buttons are touch-friendly

**8. Accessibility:**
   - Tab through navigation elements
   - Verify visible focus indicators on all buttons
   - Test skip link (Tab on chapter load)
   - Verify screen reader announces chapter titles (if available)

**9. Performance:**
   - Check chapter transitions are smooth (no jank)
   - Verify lazy sections load quickly
   - Monitor progress bar during rapid scrolling
   - Check for console errors or warnings

**Pass criteria:**
- ✅ All 6 chapters accessible and display correct content
- ✅ Navigation works via cards, prev/next, keyboard, sidebar/bottom sheet
- ✅ Progress bar fills correctly and smoothly
- ✅ Browser back/forward buttons work
- ✅ Mobile experience is usable (cards stack, navigation accessible)
- ✅ No console errors during normal usage
- ✅ Chapter transitions are smooth

**Common issues to check:**
- Progress bar stuck at 0% or 100%
- Chapter content missing or showing placeholder text
- Navigation buttons not working
- Keyboard shortcuts not responding
- Layout issues on mobile
- Console errors from lazy loading
  </how-to-verify>
  <resume-signal>
Type "approved" if all verification passed, or describe specific issues found:
- Navigation issue: [describe]
- Content issue: [describe]
- Performance issue: [describe]
- Accessibility issue: [describe]
  </resume-signal>
</task>

</tasks>

<verification>
After checkpoint approval:

1. **All chapter files created:**
   - ls src/pages/chapters/*.tsx | wc -l (should be 7: 6 chapters + index.ts)

2. **Routing integration:**
   - grep "Chapter01Introduction\|Chapter02Builder" app/App.tsx
   - grep "currentChapter ===" app/App.tsx

3. **Barrel export:**
   - grep "export.*Chapter" src/pages/chapters/index.ts

4. **Navigation flow:**
   - Manually test each navigation path
   - Verify hash changes match chapter display
   - Check browser DevTools Network tab for lazy loading

5. **No broken imports:**
   - npm run build (production build should succeed)
   - Check for unused imports
</verification>

<success_criteria>
- [ ] src/pages/chapters/index.ts exports all 6 chapters
- [ ] App.tsx routes based on currentChapter
- [ ] Landing page displays when currentChapter is null
- [ ] All 6 chapters display correct content
- [ ] Hash-based navigation works (#01-introduction, etc.)
- [ ] Chapter card navigation works
- [ ] Prev/Next buttons navigate correctly
- [ ] Keyboard shortcuts (arrow keys) work
- [ ] Browser back/forward buttons work
- [ ] Sidebar and bottom sheet navigation work
- [ ] Progress bar updates during scroll
- [ ] Chapter transitions are smooth
- [ ] Human verification checkpoint approved
- [ ] TypeScript compilation passes
- [ ] Production build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/03-chapter-reader-navigation-ux/03-04-SUMMARY.md`
</output>
