---
phase: 03-chapter-reader-navigation-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chapter/ChapterContainer.tsx
  - src/components/chapter/ChapterHeader.tsx
  - src/components/chapter/ChapterFooter.tsx
  - src/components/chapter/ChapterProgress.tsx
  - src/hooks/useReadingProgress.ts
  - src/styles/chapter.css
autonomous: true

must_haves:
  truths:
    - "Progress bar shows reading completion from 0-100%"
    - "Keyboard navigation (arrow keys) works for prev/next chapter"
    - "Chapter header displays current chapter title"
    - "Footer shows prev/next navigation buttons"
  artifacts:
    - path: "src/components/chapter/ChapterContainer.tsx"
      provides: "Wrapper component with header, footer, progress"
      min_lines: 40
    - path: "src/components/chapter/ChapterProgress.tsx"
      provides: "Fixed progress bar using useScroll"
      exports: ["ChapterProgress"]
    - path: "src/hooks/useReadingProgress.ts"
      provides: "Custom hook wrapping Motion's useScroll"
      exports: ["useReadingProgress"]
  key_links:
    - from: "src/components/chapter/ChapterProgress.tsx"
      to: "src/hooks/useReadingProgress.ts"
      via: "import useReadingProgress hook"
      pattern: "useReadingProgress"
    - from: "src/components/chapter/ChapterContainer.tsx"
      to: "contexts/NavigationContext"
      via: "useNavigation hook for current chapter"
      pattern: "useNavigation"
---

<objective>
Create chapter reading infrastructure with progress tracking, keyboard navigation, and consistent layout wrapper.

Purpose: Establish reusable chapter container system that all 6 chapters will use for consistent navigation chrome (header, footer, progress bar) while allowing flexible content composition.
Output: ChapterContainer wrapper, progress tracking system, keyboard navigation, and chapter header/footer components.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-chapter-reader-navigation-ux/03-RESEARCH.md

# Prior phase foundations
@.planning/phases/01-navigation-architecture-state-foundation/01-01-SUMMARY.md
@.planning/phases/02-landing-page-chapter-cards/02-01-SUMMARY.md

# Existing code to integrate with
@contexts/NavigationContext.tsx
@data/chapters.ts
@types/chapters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ChapterContainer wrapper and chapter header/footer components</name>
  <files>
    src/components/chapter/ChapterContainer.tsx
    src/components/chapter/ChapterHeader.tsx
    src/components/chapter/ChapterFooter.tsx
  </files>
  <action>
Create ChapterContainer.tsx as reusable wrapper for all chapter pages:
- Accept chapterId (ChapterId type) and children props
- Use useNavigation hook to get current chapter metadata
- Use getChapterById to retrieve chapter details
- Focus main content on mount for accessibility (useRef + useEffect)
- Include skip link for keyboard users (href="#main-content")
- Render structure: Skip link → ChapterHeader → main content → ChapterFooter
- Main element: id="main-content", tabIndex={-1}, aria-label with chapter title
- Min-height: 100vh on container for consistent layout

Create ChapterHeader.tsx:
- Accept title and description props
- Fixed/sticky positioning at top (position: sticky, top: 0)
- Glass morphism background (semi-transparent + backdrop-filter)
- Display chapter number and title prominently
- Include description as subtitle
- z-index: 100 to stay above content

Create ChapterFooter.tsx:
- Accept chapterId prop
- Use getNextChapter and getPrevChapter helpers
- Use navigateToChapter from useNavigation with 'sequential' type
- Render prev/next buttons with aria-label including chapter names
- Buttons only visible if prev/next chapters exist
- Glass morphism styling matching header
- Flex layout: space-between for prev (left) and next (right) alignment
- aria-label="Chapter navigation" on nav element

Use existing patterns:
- Import motion from 'framer-motion' (not 'motion/react' - per 02-02 decision)
- Glass morphism: backdrop-filter with @supports fallback (per 02-02 pattern)
- Tailwind classes for styling consistency
  </action>
  <verify>
npm run dev starts without errors
TypeScript compilation passes
ChapterContainer, ChapterHeader, ChapterFooter export successfully
  </verify>
  <done>
ChapterContainer wrapper exists with header/footer integration
ChapterHeader displays chapter title and description with sticky positioning
ChapterFooter shows prev/next navigation buttons with proper aria-labels
Skip link and accessibility features implemented
  </done>
</task>

<task type="auto">
  <name>Task 2: Create reading progress bar with useScroll hook</name>
  <files>
    src/hooks/useReadingProgress.ts
    src/components/chapter/ChapterProgress.tsx
    src/styles/chapter.css
  </files>
  <action>
Create useReadingProgress.ts custom hook:
- Import useScroll and useSpring from 'framer-motion' (not motion/react)
- Use useRef for container reference
- Call useScroll with target: containerRef, offset: ["start start", "end end"]
- Wrap scrollYProgress with useSpring for smooth animation (stiffness: 100, damping: 30, restDelta: 0.001)
- Return { containerRef, progress: smoothProgress }
- Add TypeScript return type

Create ChapterProgress.tsx component:
- Use motion.div from 'framer-motion'
- Style: position fixed, top 0, left 0, right 0, height 3px, z-index 1000
- Use scaleX transform driven by progress value (NOT width - GPU accelerated)
- transformOrigin: '0%' for left-to-right fill
- Background: var(--accent-color) or Tailwind bg-t-accent
- Add will-change: transform for performance hint
- Respect prefers-reduced-motion (use useReducedMotion hook from Phase 1)

Create chapter.css:
- CSS custom properties for chapter theming
- Progress bar styles with GPU acceleration
- Glass morphism utilities for header/footer
- Responsive spacing for chapter container
- Print styles (hide progress bar, navigation chrome)

Integrate into ChapterContainer:
- Call useReadingProgress at top of component
- Pass containerRef to root div
- Render ChapterProgress at top of component tree
- Pass progress value to ChapterProgress component
  </action>
  <verify>
npm run dev compiles successfully
TypeScript types resolve correctly
Progress bar renders at top of viewport
Console shows no errors for useScroll hook
  </verify>
  <done>
useReadingProgress hook exports containerRef and progress value
ChapterProgress component renders fixed progress bar
Progress bar uses transform: scaleX (not width) for GPU acceleration
Reduced motion preferences respected
ChapterContainer integrated with progress tracking
  </done>
</task>

<task type="auto">
  <name>Task 3: Add keyboard navigation for prev/next chapter shortcuts</name>
  <files>
    src/components/chapter/ChapterFooter.tsx
  </files>
  <action>
Add keyboard navigation to ChapterFooter component:
- Create useEffect hook that listens for keydown events
- Handle ArrowLeft key: navigate to prevChapter if it exists (navigateToChapter with 'sequential')
- Handle ArrowRight key: navigate to nextChapter if it exists (navigateToChapter with 'sequential')
- Prevent default behavior and stop propagation to avoid conflicts
- Add event listener to window, clean up on unmount
- Dependencies: [chapterId, nextChapter, prevChapter, navigateToChapter]

Add visual focus indicators:
- Ensure all buttons have :focus-visible styles
- Use ring utilities: focus-visible:ring-2 focus-visible:ring-t-accent
- Remove default outline, replace with Tailwind ring for consistency
- Test Tab navigation flow (skip link → prev button → next button)

Add keyboard hint UI (optional visual indicator):
- Small text below buttons: "Keyboard: ← → arrows"
- Hidden on mobile (md:block)
- Subtle color: text-t-secondary/60
- Only show if JavaScript enabled (progressive enhancement)

WCAG compliance:
- All interactive elements must have visible focus indicators
- aria-labels must describe destination chapter, not just "Previous/Next"
- Keyboard navigation must work without mouse
  </action>
  <verify>
npm run dev runs without errors
TypeScript compilation passes
Arrow key event listeners registered correctly
No console errors during keyboard navigation
  </verify>
  <done>
Arrow keys (left/right) navigate between chapters
Keyboard navigation uses 'sequential' type (preserves scroll)
All buttons have :focus-visible ring styles
aria-labels include chapter names
Event listeners clean up on unmount
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **ChapterContainer exports and structure:**
   - grep -r "export.*ChapterContainer" src/components/chapter/
   - Verify ChapterHeader, ChapterFooter, ChapterProgress components exist

2. **Progress tracking integration:**
   - grep "useReadingProgress" src/components/chapter/ChapterContainer.tsx
   - grep "useScroll.*useSpring" src/hooks/useReadingProgress.ts
   - Verify scaleX usage (not width) in ChapterProgress

3. **Keyboard navigation:**
   - grep "ArrowLeft\|ArrowRight" src/components/chapter/ChapterFooter.tsx
   - grep "addEventListener.*keydown" src/components/chapter/ChapterFooter.tsx

4. **Accessibility:**
   - grep "aria-label" src/components/chapter/
   - grep "skip-link\|main-content" src/components/chapter/ChapterContainer.tsx
   - grep ":focus-visible" src/components/chapter/ src/styles/chapter.css
</verification>

<success_criteria>
- [ ] ChapterContainer.tsx exists and exports component
- [ ] ChapterHeader and ChapterFooter render with glass morphism
- [ ] ChapterProgress bar uses transform: scaleX for GPU acceleration
- [ ] useReadingProgress hook wraps useScroll with spring smoothing
- [ ] Keyboard shortcuts (arrow keys) navigate chapters
- [ ] All interactive elements have :focus-visible styles
- [ ] Skip link exists for keyboard users
- [ ] TypeScript compilation passes with no errors
- [ ] Dev server starts successfully
</success_criteria>

<output>
After completion, create `.planning/phases/03-chapter-reader-navigation-ux/03-01-SUMMARY.md`
</output>
