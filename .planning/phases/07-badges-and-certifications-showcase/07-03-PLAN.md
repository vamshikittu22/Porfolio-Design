---
phase: 07-badges-and-certifications-showcase
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - sections/badges/jsonld.ts
  - sections/badges/BadgeShowcase.tsx
  - sections/badges/BadgeDetailDrawer.tsx
  - components/seo/SEOManager.tsx
  - services/badges/verifyProof.ts
  - services/badges/index.ts
  - services/chatService.ts
  - components/layout/ChatAssistant/ChatAssistant.tsx
autonomous: true
must_haves:
  truths:
    - "Search engines and AI crawlers receive Schema.org + Open Badge JSON-LD for every credential when Chapter 7 loads."
    - "Badge cards and drawers surface verification status (active/expired/revoked/pending) sourced from the proof service."
    - "ChatAssistant can summarize badges/certifications because chatService injects structured badge summaries into the system prompt and adds Chapter 7 suggestion chips."
  artifacts:
    - path: "sections/badges/jsonld.ts"
      provides: "Helpers to build EducationalOccupationalCredential JSON-LD payloads"
    - path: "services/badges/verifyProof.ts"
      provides: "verifyBadgeProof() returning BadgeStatus + proof metadata"
    - path: "services/chatService.ts"
      provides: "Badge summaries appended to AI system instruction"
  key_links:
    - from: "components/seo/SEOManager.tsx"
      to: "sections/badges/jsonld.ts"
      via: "inject badge JSON-LD scripts when currentChapter === '07-badges'"
      pattern: "buildCredentialJsonLd"
    - from: "sections/badges/BadgeDetailDrawer.tsx"
      to: "services/badges/index.ts"
      via: "renders status/proof info from BadgeRecord"
      pattern: "badge.verification"
    - from: "components/layout/ChatAssistant/ChatAssistant.tsx"
      to: "services/chatService.ts"
      via: "new Chapter 7 suggestion set"
      pattern: "'07-badges'"
---

<objective>
Wire verification, structured data, and AI context around the new badge showcase so credentials are trustworthy to both humans and machines.

Purpose: Ensures Chapter 7 delivers SEO/AI value (JSON-LD), communicates proof status in the UI, and allows the navigation assistant to reason about certifications.
Output: JSON-LD builders, SEOManager updates, verification-enriched UI, and AI prompt enhancements referencing badge data.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/07-badges-and-certifications-showcase/07-RESEARCH.md
@services/chatService.ts
@components/seo/SEOManager.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit Schema.org + Open Badge JSON-LD for Chapter 7</name>
  <files>sections/badges/jsonld.ts
sections/badges/BadgeShowcase.tsx
components/seo/SEOManager.tsx
sections/badges/BadgeDetailDrawer.tsx</files>
  <action>
    - Create `sections/badges/jsonld.ts` exporting `buildCredentialJsonLd(badge: BadgeRecord)` and `buildOpenBadgeJson(badge: BadgeRecord)` helpers (use `jsonld` package to compact contexts, map skills to `DefinedTerm`, embed issuer profiles, criteria narratives, verification URLs, etc.).
    - Update `sections/badges/BadgeShowcase.tsx` to memoize JSON-LD payloads for the currently visible badges and expose functions for the detail drawer (download JSON-LD, copy proof link).
    - Augment `sections/badges/BadgeDetailDrawer.tsx` with action buttons: "Download JSON-LD" (creates blob from helper) and "View RAW Credential" (links to issuer id) and show the Schema.org snippet when `prefersReducedMotion`? (Keep accessible).
    - Enhance `components/seo/SEOManager.tsx` to detect `currentChapter === '07-badges'`, iterate through badge JSON-LD payloads, and inject `<script id="badge-schema-{id}">` nodes so crawlers/AI companion can discover certifications; clean up scripts when leaving the chapter.
  </action>
  <verify>Run `npm run build` to ensure SEOManager + new helpers type-check, then in devtools verify `<script type="application/ld+json" id="badge-schema-*">` exist on Chapter 7.</verify>
  <done>Every badge emits valid JSON-LD (downloadable + injected into head) whenever Chapter 7 is active.</done>
</task>

<task type="auto">
  <name>Task 2: Surface verification results in UI and AI context</name>
  <files>services/badges/verifyProof.ts
services/badges/index.ts
sections/badges/BadgeShowcase.tsx
sections/badges/BadgeDetailDrawer.tsx
services/chatService.ts
components/layout/ChatAssistant/ChatAssistant.tsx</files>
  <action>
    - Extend `verifyBadgeProof` to cache `credentialStatus` fetches, capture proof method (DataIntegrityProof vs VC-JOSE), set `BadgeStatus` enums, and include human-readable `verificationLabel` + `verifiedAt`.
    - Update `services/badges/index.ts` so `loadBadgeRecords()` attaches `verification` metadata (status, method, lastChecked) and a short `aiSummary` string for each badge (title + issuer + issuedOn + skill cluster + verification URL).
    - Render verification chips + status tooltips in `BadgeShowcase` cards and `BadgeDetailDrawer` (color-coded for active/expired/revoked, fallback text for pending). Include CTA to refetch status on demand (calls verify service).
    - Enhance `services/chatService.ts` `getChapterContext` (or new helper) to append the badge summaries when `currentChapter === '07-badges'`, ensuring AI references issuer names, skills, and proof links.
    - Update `components/layout/ChatAssistant/ChatAssistant.tsx` `SUGGESTIONS` map with Chapter 7 prompts (e.g., "Certifications", "Verify a badge", "Skills proven"), enabling quick navigation queries about badges.
  </action>
  <verify>Run `npm run test -- badges` to cover new verification code, then open Chapter 7 and confirm status chips update + ChatAssistant shows Chapter 7 suggestions.</verify>
  <done>UI reflects real verification status, AI prompt includes badge summaries, and quick suggestions guide users through the new chapter.</done>
</task>

</tasks>

<verification>
- `npm run build` + `npm run test -- badges` both succeed.
- Browser inspection shows JSON-LD scripts injected on Chapter 7 and status chips change when proof data updates.
- ChatAssistant suggestions list Chapter 7 actions and replies mention badges when asked.
</verification>

<success_criteria>
- Structured data for badges is machine-readable and downloadable.
- Verification outcomes are visible and trustworthy in the UI.
- AI companion uses badge summaries to answer navigation questions.
</success_criteria>

<output>
After execution, create `.planning/phases/07-badges-and-certifications-showcase/07-03-SUMMARY.md`.
</output>
