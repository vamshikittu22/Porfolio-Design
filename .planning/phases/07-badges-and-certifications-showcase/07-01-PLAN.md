---
phase: 07-badges-and-certifications-showcase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - data/badges/schemas.ts
  - data/badges/badgeContent.ts
  - data/badges/issuers.ts
  - data/badges/normalize.ts
  - services/badges/index.ts
  - tests/badges/badgeSchemas.test.ts
autonomous: true
must_haves:
  truths:
    - "Badge payloads cannot enter the UI unless they pass OpenBadgeCredential schema validation."
    - "Each credential exposes issuer profile, achievement criteria, and status metadata in a normalized view model."
    - "Chapter 7 components can call loadBadgeRecords() to retrieve curated, validated BadgeRecord data without hitting remote issuers."
  artifacts:
    - path: "data/badges/schemas.ts"
      provides: "zod schemas and inferred types for OpenBadgeCredential plus BadgeRecord view models"
    - path: "data/badges/badgeContent.ts"
      provides: "Seed OpenBadgeCredential JSON array consumed during development"
    - path: "services/badges/index.ts"
      provides: "loadBadgeRecords() API that returns validated local badge data with status details"
    - path: "tests/badges/badgeSchemas.test.ts"
      provides: "Vitest coverage preventing regressions in badge schema parsing"
  key_links:
    - from: "services/badges/index.ts"
      to: "data/badges/badgeContent.ts"
      via: "loadBadgeRecords pipeline"
      pattern: "BADGE_CREDENTIALS"
    - from: "services/badges/index.ts"
      to: "data/badges/schemas.ts"
      via: "badgeCredentialSchema.parse"
      pattern: "badgeCredentialSchema"
---

<objective>
Establish a standards-compliant badge data pipeline with schema validation, normalized view models, and remote fetch hooks so later plans can focus on UI and AI wiring.

Purpose: Guarantees every credential follows Open Badges 3.0 / VC 2.0 requirements, exposes issuer metadata, and fails fast during CI if data is malformed.
Output: New badge schemas, curated credential JSON, reusable loadBadgeRecords() helper, and Vitest coverage.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-badges-and-certifications-showcase/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install credential toolchain and scaffold badge schemas</name>
  <files>package.json
package-lock.json
  <action>
    - Add `zod@4.3.6`, `date-fns@4.1.0`, `@heroicons/react@2.2.0`, and `jsonld@8.x` to `dependencies`, run `npm install`, and commit the updated lockfile.
    - Create `data/badges/schemas.ts` with OpenBadgeCredential + BadgeIssuer + BadgeEvidence zod objects that match the IMS spec (context array, `type` includes `OpenBadgeCredential`, issuer DID URL, credentialSubject.achievement fields, optional evidence array, proof array, credentialStatus, validFrom/validUntil fields).
    - Derive strongly typed helpers: `BadgeCredential`, `BadgeRecord`, `BadgeSkill`, `BadgeStatus` union (active/expired/revoked/pending), plus a `badgeRecordSchema` transform that flattens issuance dates using `date-fns/format`.
    - Export utility guards (`isBadgeCredential`, `coerceBadgeStatus`) so downstream modules can gate unknown payloads before rendering.
  </action>
  <verify>Run `npm run test -- --watch=false` to ensure TypeScript builds and no existing tests fail after dependency install.</verify>
  <done>`data/badges/schemas.ts` exports validated schemas/types and the project builds with new deps installed.</done>
</task>

<task type="auto">
  <name>Task 2: Seed verifiable credential content and normalization helpers</name>
  <files>data/badges/badgeContent.ts
data/badges/issuers.ts
data/badges/normalize.ts
services/badges/index.ts</files>
  <action>
    - Create `data/badges/badgeContent.ts` containing 4-6 curated OpenBadgeCredential JSON objects that cover multiple issuers, different skill clusters, `credentialStatus` URLs, `proof` blocks, and evidence links.
    - Create `data/badges/issuers.ts` with cached issuer metadata (name, URL, logo, verification keys) keyed by issuer DID to avoid duplicating profile details.
    - Add `data/badges/normalize.ts` with pure helpers that map an OpenBadgeCredential into a `BadgeRecord` (id, title, summary, issuer metadata, issuedOn/expiresOn, skills, verification URL) plus derived booleans for `isExpired`/`isRevoked`.
    - Build `services/badges/index.ts` that exposes `BADGE_CREDENTIALS`, `listBadgeIssuers()`, `normalizeCredential()`, and a `loadBadgeRecords()` function returning cached `BadgeRecord[]` by piping each credential through `badgeCredentialSchema.parse` and `buildBadgeRecord`.
  </action>
  <verify>Temporarily import `getLocalBadgeRecords()` in a scratch script or `ts-node` REPL to ensure it returns at least 4 normalized records with issuer + status data.</verify>
  <done>`getLocalBadgeRecords()` returns validated BadgeRecord objects built from the curated credential JSON without throwing schema errors.</done>
</task>

<task type="auto">
  <name>Task 3: Add Vitest coverage for schemas and normalization</name>
  <files>tests/badges/badgeSchemas.test.ts</files>
  <action>
    - Create `tests/badges/badgeSchemas.test.ts` (Vitest) covering: successful parsing of a valid credential, failure when issuer URL missing, and normalization producing `BadgeRecord` objects with issuer/date/skill data.
    - Mock `BADGE_CREDENTIALS` inputs to ensure schema errors throw helpful messages and transforms format dates via `date-fns` utilities.
    - Add an assertion that `loadBadgeRecords()` returns at least four records and strips proof arrays before exposing data to the UI (ensuring privacy-friendly view models).
  </action>
  <verify>Run `npm run test -- badges` (or `npx vitest run tests/badges/badgeSchemas.test.ts`) and ensure the new suite passes.</verify>
  <done>Vitest suite covers schema validation + normalization, and the test command passes.</done>
</task>

</tasks>

<verification>
- `npm run test -- badges` passes with the new schema suite.
- `loadBadgeRecords()` returns ≥4 BadgeRecord objects, each containing issuer, criteria, status, and proof metadata.
</verification>

<success_criteria>
- Badge data model follows Open Badges 3.0 + VC 2.0 fields with zod validation.
- Local + remote badge sources share a single normalization pipeline with verification hooks.
- Automated tests prevent regressions in badge parsing/verification.
</success_criteria>

<output>
After execution, create `.planning/phases/07-badges-and-certifications-showcase/07-01-SUMMARY.md`.
</output>
