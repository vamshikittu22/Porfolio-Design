---
phase: 07-badges-and-certifications-showcase
plan: 04
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - types/chapters.ts
  - data/chapters.ts
  - data/chapterContent.ts
  - components/navigation/HeaderNav.tsx
  - src/pages/LandingPage.tsx
  - app/App.tsx
  - config/seo.ts
  - src/styles/chapter.css
autonomous: true
must_haves:
  truths:
    - "Chapter 7 appears in landing cards, header/sidebar navigation, and copy with accurate numbering and descriptions."
    - "Selecting Chapter 7 through navigation routes the user to Chapter07Badges without console errors."
    - "Theme tokens and SEO metadata exist for the new chapter (accent color, OG tags, keywords)."
  artifacts:
    - path: "types/chapters.ts"
      provides: "ChapterId union + metadata extended with '07-badges'"
    - path: "app/App.tsx"
      provides: "Routing entry that renders Chapter07Badges when currentChapter === '07-badges'"
    - path: "config/seo.ts"
      provides: "CHAPTER_SEO entry describing badges & certifications"
  key_links:
    - from: "components/navigation/HeaderNav.tsx"
      to: "types/chapters.ts"
      via: "CHAPTER_NAV entry referencing '07-badges'"
      pattern: "'07-badges'"
    - from: "app/App.tsx"
      to: "src/pages/chapters/Chapter07Badges.tsx"
      via: "Chapter routing"
      pattern: "<Chapter07Badges"
    - from: "src/styles/chapter.css"
      to: "[data-chapter='07-badges']"
      via: "theme token selectors"
      pattern: "07-badges"
---

<objective>
Wire the new badges chapter into landing navigation, routing, theming, and SEO metadata so users can discover it and search engines understand the new storyline.

Purpose: Guarantees the badge showcase is reachable from every navigation surface and inherits unique accent styling and page metadata.
Output: Updated chapter registry/types, navigation copy, routing, SEO config, and chapter-specific CSS tokens.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-badges-and-certifications-showcase/07-RESEARCH.md
@app/App.tsx
@components/navigation/HeaderNav.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend chapter registry and navigation copy</name>
  <files>types/chapters.ts
data/chapters.ts
components/navigation/HeaderNav.tsx
src/pages/LandingPage.tsx</files>
  <action>
    - Add `'07-badges'` to `ChapterId` and expand `CHAPTERS` metadata (title "The Credible", subtitle, summary, icon) with skill clusters for the badge story.
    - Update `data/chapterContent.ts` to include Chapter 7 sections (Badges, Evidence, Verification) and AI prompt copy referencing verifiable credentials.
    - Inject a Chapter 7 entry into `HeaderNav` / sidebar toggle arrays with hero icon + accent color so users can jump directly via nav.
    - Refresh landing hero text and chapter list (e.g., "Seven chapters") and add a badges card summary + CTA in `src/pages/LandingPage.tsx`.
  </action>
  <verify>Run `npm run build` then load the landing page to confirm text references seven chapters and the Chapter 7 card/nav link appears.</verify>
  <done>Chapter registry + content data include the badges story, and navigation surfaces expose a Chapter 7 entry.</done>
</task>

<task type="auto">
  <name>Task 2: Wire routing, theming, and SEO metadata</name>
  <files>app/App.tsx
config/seo.ts
src/styles/chapter.css</files>
  <action>
    - Import `Chapter07Badges` in `app/App.tsx` and update the chapter switcher/hash routing so selecting `'07-badges'` renders the new page and updates document titles.
    - Add `CHAPTER_SEO['07-badges']` entry with title, description, keywords, OG image, and update helper functions (e.g., `getChapterSeo`) to include it.
    - Introduce `[data-chapter="07-badges"]` accent tokens (e.g., amber gradient, badge icons) in `src/styles/chapter.css`, plus CSS variables for glass overlays matching the new palette.
  </action>
  <verify>Run `npm run build` and smoke-test `/` + `/#07-badges` to ensure routing works, theme colors apply, and document meta tags reflect Chapter 7.</verify>
  <done>Routing, SEO, and theming recognize Chapter 7 so the badge showcase feels first-class.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds after navigation/theming updates.
- Manual check: landing page shows seven chapters, header nav includes Chapter 7, and `/#07-badges` loads the badge showcase with the new accent colors.
</verification>

<success_criteria>
- Chapter registry + copy reference the badges storyline across landing and navigation.
- Routing and SEO metadata include Chapter 7, ensuring direct hash links and crawlable context.
- Theme tokens exist so badge layouts inherit the intended gold accent.
</success_criteria>

<output>
After execution, create `.planning/phases/07-badges-and-certifications-showcase/07-04-SUMMARY.md`.
</output>
