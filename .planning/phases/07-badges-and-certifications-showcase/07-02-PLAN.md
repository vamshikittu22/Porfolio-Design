---
phase: 07-badges-and-certifications-showcase
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/pages/chapters/Chapter07Badges.tsx
  - src/pages/chapters/index.ts
  - sections/badges/BadgeShowcase.tsx
  - sections/badges/BadgeDetailDrawer.tsx
  - sections/badges/CredentialFilters.tsx
  - sections/badges/hooks/useBadgeFilters.ts
  - sections/badges/index.ts
  - sections/badges/badge-showcase.css
autonomous: true
must_haves:
  truths:
    - "Visitors can browse badges in a responsive, animated glass grid with filter chips and see details without leaving the chapter."
    - "Each badge detail drawer shows issuance dates, issuer profile, criteria, verification CTA, and badge status pulled from loadBadgeRecords()."
    - "Chapter07Badges page composes the badge showcase with storytelling copy and exports through the chapters barrel."
  artifacts:
    - path: "src/pages/chapters/Chapter07Badges.tsx"
      provides: "ChapterContainer composition for the Badges & Certifications story"
    - path: "sections/badges/BadgeShowcase.tsx"
      provides: "Interactive grid UI backed by Framer Motion layout groups"
    - path: "sections/badges/hooks/useBadgeFilters.ts"
      provides: "Stateful filtering logic shared by chips and grid"
  key_links:
    - from: "Chapter07Badges.tsx"
      to: "sections/badges/BadgeShowcase.tsx"
      via: "renders BadgeShowcase inside ChapterContainer"
      pattern: "<BadgeShowcase"
    - from: "sections/badges/BadgeShowcase.tsx"
      to: "services/badges/index.ts"
      via: "loadBadgeRecords"
      pattern: "loadBadgeRecords"
---

<objective>
Ship the Chapter 7 experience with a responsive badge showcase UI and wire it into the existing navigation/theming system so visitors can explore verifiable credentials like any other chapter.

Purpose: Turns badge data from Plan 07-01 into a tangible chapter storyline while keeping navigation, SEO, and glass morphism patterns consistent.
Output: Chapter07Badges page, badge showcase components (grid, filters, detail drawer), updated navigation/SEO/theme metadata for the seventh chapter.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-badges-and-certifications-showcase/07-RESEARCH.md
@app/App.tsx
@src/pages/chapters/Chapter02Builder.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build badge showcase component system</name>
  <files>sections/badges/BadgeShowcase.tsx
sections/badges/BadgeDetailDrawer.tsx
sections/badges/CredentialFilters.tsx
sections/badges/hooks/useBadgeFilters.ts
sections/badges/index.ts
sections/badges/badge-showcase.css</files>
  <action>
    - Create `sections/badges/BadgeShowcase.tsx` that pulls `loadBadgeRecords()` (from Plan 07-01), runs them through `useBadgeFilters`, and renders a responsive CSS Grid (auto-fit minmax) of glass cards using Framer Motion `layoutGroup` for animated reflow.
    - Implement `sections/badges/hooks/useBadgeFilters.ts` to manage filter state (skill clusters, issuers, status toggles) with derived sets and memoized selectors so filtering stays under 16ms on mobile.
    - Build `sections/badges/CredentialFilters.tsx` for pill buttons with 44px min touch targets, reduced motion fallbacks, and `@heroicons/react` icons indicating active filters.
    - Create `sections/badges/BadgeDetailDrawer.tsx` as a motion-driven overlay/drawer that shows badge artwork, issuer metadata, evidence links, status chips, and CTA buttons ("Verify on Issuer", "Download JSON-LD") fed by the selected `BadgeRecord`.
    - Add `sections/badges/badge-showcase.css` for glass overlays, Masonry fallback (column-count) when CSS Grid gaps collapse on older browsers, and theme-aware gradients that hook into `--chapter-accent` variables.
    - Export everything via `sections/badges/index.ts` for reuse across Chapter07 orchestration.
  </action>
  <verify>Run `npm run build` to ensure the new components type-check, then start `npm run dev` and confirm the badge grid renders with filters + detail drawer in Chapter07.</verify>
  <done>Chapter07 renders a functional badge showcase with working filters, animated cards, and detail drawer powered by loadBadgeRecords().</done>
</task>

<task type="auto">
  <name>Task 2: Compose Chapter07 page and export it through chapters barrel</name>
  <files>src/pages/chapters/Chapter07Badges.tsx
src/pages/chapters/index.ts</files>
  <action>
    - Compose `src/pages/chapters/Chapter07Badges.tsx` with `ChapterContainer`, the new `BadgeShowcase`, introductory copy, stats about earned certifications, and lazy suspense boundaries for evidence drawers.
    - Ensure the page wires props for hero CTA, summary tiles, and `data-chapter="07-badges"` to pick up theme tokens once registered in a later plan.
    - Update `src/pages/chapters/index.ts` to export `Chapter07Badges` for routing/use by `app/App.tsx` and upcoming navigation work.
  </action>
  <verify>Run `npm run build` (or `npm run lint && npm run build`) to confirm Chapter07 compiles and the barrel exports succeed.</verify>
  <done>Chapter07Badges page renders the badge showcase, and other modules can import it via the chapters barrel.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds after both tasks.
- Manual smoke test: open `/#07-badges`, ensure the badge grid renders, filters work, and detail drawers open without console errors (landing/nav wiring will be handled in a subsequent plan).
</verification>

<success_criteria>
- Badge showcase UI offers responsive grid, filters, and modal-level detail consistent with existing glass morphism patterns.
- Chapter07Badges page composes the new experience with storytelling copy ready for navigation wiring.
- Components compile cleanly with the rest of the app, enabling later plans to hook into navigation/SEO/theming.
</success_criteria>

<output>
After execution, create `.planning/phases/07-badges-and-certifications-showcase/07-02-SUMMARY.md`.
</output>
